# Core dependencies (pinned for reproducibility)
python-dotenv==1.2.3
requests==2.34.2
pandas==2.2.3
numpy==2.1.3
ta==0.11.0
openai==1.58.1
yfinance==0.2.49

# Discord bot
discord.py==2.5.2

# Database (SQLite3 is included with Python)

# Machine Learning (ML strategy: core/ml_strategy.py, scripts/train_model.py)
scikit-learn==1.6.0
joblib==1.4.2
lightgbm==4.7.0
# FinBERT news sentiment is optional (large download): pip install -r requirements-ml.txt
# Without it, a built-in lexicon scorer is used.

# Data visualization (for backtesting)
matplotlib==3.10.0
seaborn==0.13.2

# Additional utilities
python-dateutil==2.9.0
pytz==2025.1

# API Server dependencies (updated to latest stable versions)
fastapi==0.141.1
uvicorn[standard]==0.34.0
# Real-time order updates (core/stream_listener.py); also pulled in by uvicorn[standard]
websockets==17.1
pydantic==2.10.5
pydantic-settings==2.7.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.32
PyJWT==2.15.0

# Rate limiting for API endpoints
slowapi==0.1.9

# Test and quality tooling
pytest==9.1.1
pytest-asyncio==1.3.0

# Security scanning (development)
# Uncomment for security checks: pip install -r requirements-dev.txt
# bandit==1.7.10
# safety==3.2.11
